import LoadingComponent from "@/components/Loading";

const Loading = () => (
  <div className="w-screen h-screen  flex justify-center items-center text-2xl z-50">
    <LoadingComponent absloute />
  </div>
);

export default Loading;
